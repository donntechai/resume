# Resume Website

This is a simple, professional resume website generated from `LM_resume.docx` content.

## Files

- `index.html`: page content
- `styles.css`: styling (responsive + print-friendly)

## Local preview

Open `index.html` in your browser.

## Publish with GitHub Pages

In the GitHub repo:

- Go to **Settings → Pages**
- **Build and deployment**: Deploy from a branch
- **Branch**: `main` / `(root)`

## Update contact info

If you want email/phone/LinkedIn shown in the header, tell me what to add and I’ll wire it in cleanly (with `mailto:` / links).

